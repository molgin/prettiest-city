<h1>Global City Rankings</h1>

<ol>

<% City.sort_by_win_ratio.each do |city| %>

  <li>
    <strong><%= city.name %>, <%= city.state.abbr if city.state %></strong><br>won <%= city.win_percentage_display %> of <%= city.total %> matchups
  </li>

<% end %>

</ol>